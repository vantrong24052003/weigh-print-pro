# Nhiệm Vụ (Tasks)

- [x] Tạo tập tài liệu `notebooks/s2-qz-tray-integration-and-linux-fix.md` nói rõ cơ chế tắt AppArmor, và ép USB cổng cứng (`lpadmin`).
- [x] Xoá đi mọi rác `useSerialPrinter.ts` cũ của kiến trúc Web Serial.
- [x] Sửa Hook `useQzPrinter.ts` bằng mã 3 dòng vượt lỗi Chứng chỉ Số (setCertificatePromise).
- [x] Update hàm `qz.print` mã hoá ngược `Uint8Array` ra hệ `hex` đẩy format JSON cho in mượt.
- [x] Sửa Component báo in (Giao diện) để chừa 1 cái nút Độc Tôn "In Nhanh QZ Tray" vàng chói. Bỏ cái nút Print thường ảo đi (Xóa luôn style rác CSS `App.css`).
