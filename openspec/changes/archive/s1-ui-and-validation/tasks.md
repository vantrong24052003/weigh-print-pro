# Nhiệm Vụ (Tasks)

- [x] Tạo file Schema `src/schemas/weighingSchema.ts` dùng cho Yup.
- [x] Hoàn thiện Component `src/components/weighing-report/AxleRow.tsx`.
- [x] Tạo Form nhập trên React với Tailwind CSS `src/App.tsx`.
- [x] Gắn các state lưu trữ lỗi và ràng buộc `.catch(yup.ValidationError)` cho `handlePrint` và `handleShowPreview`.
- [x] Dùng React Hooks (`useMemo`) tính tổng số kg trên các trục xe thời gian thực.
